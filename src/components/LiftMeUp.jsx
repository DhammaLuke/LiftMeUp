const LiftMeUp = ({liftMessage}) => (
  <div className="liftMeUp">
    <div className="liftMeUpBody">
      {liftMessage}
    </div>
    <button type="button" className="btn btn-primary">Lift Me Up</button>
  </div>
);
